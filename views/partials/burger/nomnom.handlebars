<li>
    {{burger_name}}

    <button class="change-devoured luckyfont btn btn-sm btn-outline-primary" data-id="{{id}}" data-devoured="{{devoured}}">
        {{#if devoured}}EAT ME!{{else}}DONT CHOMP{{/if}}
    </button>
</li>